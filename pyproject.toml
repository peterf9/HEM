[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hem"
version = "0.1.0"
description = "HEM Project"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
hem = "hem.cli:main"
